#include <stdbool.h>

struct edge {
    int from;
    int to;
};

#include "Graph.h"

bool isEulerPath(Graph g, struct edge e[], int nE);



bool isEulerPath(Graph g, struct edge e[], int nE) {
    return false;
}